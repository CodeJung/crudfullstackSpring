package com.exemplo.crudmongo.Model;

public enum Role {
    //Aqui coloquei um enum contendo as roles que teremos no sistema e la no Pessoa eu apenas referencio esse enum
    ROLE_COORDERNADOR,
    ROLE_ALUNO
}
